<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
    bootstrap="tests/bootstrap.php"
    colors="true">
    <testsuites>
        <testsuite name="SaveTests">
            <directory>tests</directory>
        </testsuite>
    </testsuites>

    <source>
        <include>
            <!-- Only include directories with testable code that have tests -->
            <directory suffix=".php">./save</directory>
            <directory suffix=".php">./includes</directory>
            <directory suffix=".php">./api/v2</directory>
            <directory suffix=".php">./endpoints</directory>
            <directory suffix=".php">./scripts</directory>
        </include>
        <exclude>
            <!-- Exclude save_data.php as it's mainly an orchestration file -->
            <file>./save/save_data.php</file>
        </exclude>
    </source>
</phpunit>