# Enforce Unix (LF) line endings for shell scripts to ensure
# they are executable in Linux-based Docker containers.
*.sh		text eol=lf
# Enforce Unix (LF) line endings for environment files to ensure
# consistent processing across platforms and in Docker containers.
*.env		text eol=lf
.env*		text eol=lf