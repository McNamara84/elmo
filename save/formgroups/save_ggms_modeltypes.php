<?php 

a place to record the variables specific to each and every model type: temporal, static, topographic.
Possibly othher types in the future.
 * Saves the Gravity Gradient Model (GGM) model types to the database.
 *
 * @param mysqli $connection  The database connection.
 * @param array  $postData    The POST data from the form.
 * @param int    $resource_id The ID of the associated resource.
 *
 * @return void
 */